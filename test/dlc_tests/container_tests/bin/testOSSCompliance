
FILES=("/root/PYTHON_PACKAGES_LICENSES"  "/root/LINUX_PACKAGES_LICENSES" "/root/BUILD_FROM_SOURCE_PACKAGES_LICENCES")

for FILE in ${FILES[@]}; do
    if [ -f "$FILE" ]; then
        echo "$FILE exists."
    else
        exit 1
    fi
done
