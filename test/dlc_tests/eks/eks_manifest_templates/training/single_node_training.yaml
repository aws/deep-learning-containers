---
apiVersion: v1
kind: Pod
metadata:
  name: <POD_NAME>
spec:
  restartPolicy: OnFailure
  containers:
  - name: <POD_NAME>
    image: <CONTAINER_NAME>
    command:
      - "/bin/sh"
      - "-c"
    args:
    - <ARGS>
    resources:
      limits:
        cpu: <CPU_LIMIT>
        ephemeral-storage: "1Gi"
        nvidia.com/gpu: "1"
      requests:
        ephemeral-storage: "500Mi"
