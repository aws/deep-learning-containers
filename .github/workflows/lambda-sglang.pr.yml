name: "PR - Lambda SGLang"

on:
  pull_request:
    branches: [lambda]
    types: [opened, reopened, synchronize]
    paths:
      - ".github/config/image/lambda/sglang/*.yml"
      - ".github/workflows/lambda.pipeline.yml"
      - ".github/workflows/lambda-sglang.pr.yml"
      - ".github/workflows/sglang.tests-upstream.yml"
      - "docker/lambda/**"
      - "scripts/docker/lambda/sglang/**"
      - "scripts/common/**"
      - "scripts/telemetry/**"
      - "test/lambda/**"
      - "test/sanity/**"
      - "test/security/data/ecr_scan_allowlist/lambda/**"
      - "test/telemetry/**"
      - "!docs/**"

permissions:
  contents: read
  pull-requests: read

jobs:
  gatekeeper:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          fetch-depth: 1
      - uses: ./.github/actions/pr-permission-gate

  check-changes:
    needs: [gatekeeper]
    runs-on: ubuntu-latest
    outputs:
      build-change: ${{ steps.changes.outputs.build-change }}
      validation-test-change: ${{ steps.changes.outputs.validation-test-change }}
      upstream-test-change: ${{ steps.changes.outputs.upstream-test-change }}
      sanity-test-change: ${{ steps.changes.outputs.sanity-test-change }}
      telemetry-test-change: ${{ steps.changes.outputs.telemetry-test-change }}
    steps:
      - uses: actions/checkout@v6
      - uses: dorny/paths-filter@v4
        id: changes
        with:
          filters: |
            build-change:
              - ".github/workflows/lambda-sglang.pr.yml"
              - ".github/config/image/lambda/sglang/*.yml"
              - "docker/lambda/**"
              - "scripts/docker/lambda/sglang/**"
              - "scripts/common/**"
              - "test/security/data/ecr_scan_allowlist/lambda/**"
            validation-test-change:
              - "test/lambda/**"
            upstream-test-change:
              - ".github/workflows/sglang.tests-upstream.yml"
            sanity-test-change:
              - "test/sanity/**"
            telemetry-test-change:
              - "scripts/telemetry/**"
              - "test/telemetry/**"

  discover:
    needs: [gatekeeper]
    runs-on: ubuntu-latest
    outputs:
      configs: ${{ steps.discover.outputs.configs }}
    steps:
      - uses: actions/checkout@v6
      - id: discover
        uses: ./.github/actions/discover-configs
        with:
          pattern: ".github/config/image/lambda/sglang/*.yml"

  pipeline:
    needs: [check-changes, discover]
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJson(needs.discover.outputs.configs) }}
    uses: ./.github/workflows/lambda.pipeline.yml
    with:
      config-file: ${{ matrix.config_file }}
      tag-suffix: pr-${{ github.event.pull_request.number }}
      build: ${{ needs.check-changes.outputs.build-change == 'true' }}
      run-sanity-test: ${{ needs.check-changes.outputs.build-change == 'true' || needs.check-changes.outputs.sanity-test-change == 'true' }}
      run-security-test: ${{ needs.check-changes.outputs.build-change == 'true' }}
      run-telemetry-test: ${{ needs.check-changes.outputs.build-change == 'true' || needs.check-changes.outputs.telemetry-test-change == 'true' }}
      run-validation-test: ${{ needs.check-changes.outputs.build-change == 'true' || needs.check-changes.outputs.validation-test-change == 'true' }}
      run-sglang-upstream-test: ${{ needs.check-changes.outputs.build-change == 'true' || needs.check-changes.outputs.upstream-test-change == 'true' }}
      release: false
    secrets: inherit
