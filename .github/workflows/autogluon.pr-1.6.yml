name: "PR - AutoGluon 1.6"

on:
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize]
    paths:
      - ".github/config/image/autogluon/1.6-sagemaker-*.yml"
      - ".github/workflows/autogluon.pipeline.yml"
      - ".github/workflows/autogluon.pr-1.6.yml"
      - ".github/workflows/autogluon.tests-sagemaker.yml"
      - ".github/workflows/autogluon.tests-unit.yml"
      - "docker/autogluon/**"
      - "scripts/docker/autogluon/**"
      - "scripts/docker/common/setup_oss_compliance.sh"
      - "scripts/docker/telemetry/**"
      - "test/sanity/**"
      - "test/security/data/ecr_scan_allowlist/autogluon/**"
      - "test/telemetry/**"
      - "test/autogluon/**"
      - "!docs/**"

permissions:
  contents: read
  pull-requests: read

jobs:
  gatekeeper:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          fetch-depth: 1
      - uses: ./.github/actions/pr-permission-gate

  check-changes:
    needs: [gatekeeper]
    runs-on: ubuntu-latest
    outputs:
      build-change: ${{ steps.changes.outputs.build-change }}
      sanity-test-change: ${{ steps.changes.outputs.sanity-test-change }}
      unit-test-change: ${{ steps.changes.outputs.unit-test-change }}
      sagemaker-test-change: ${{ steps.changes.outputs.sagemaker-test-change }}
      telemetry-test-change: ${{ steps.changes.outputs.telemetry-test-change }}
    steps:
      - uses: actions/checkout@v6
      - uses: dorny/paths-filter@v4
        id: changes
        with:
          filters: |
            build-change:
              - ".github/config/image/autogluon/1.6-sagemaker-*.yml"
              - "docker/autogluon/**"
              - "scripts/docker/autogluon/**"
              - "scripts/docker/common/setup_oss_compliance.sh"
              - "test/security/data/ecr_scan_allowlist/autogluon/**"
            sanity-test-change:
              - "test/sanity/**"
            unit-test-change:
              - "scripts/docker/autogluon/**"
              - "test/autogluon/inference/test_*.py"
              - ".github/workflows/autogluon.tests-unit.yml"
            sagemaker-test-change:
              - "test/autogluon/sagemaker/**"
              - ".github/workflows/autogluon.tests-sagemaker.yml"
            telemetry-test-change:
              - "scripts/docker/telemetry/**"
              - "test/telemetry/**"

  discover:
    needs: [gatekeeper]
    runs-on: ubuntu-latest
    outputs:
      configs: ${{ steps.discover.outputs.configs }}
    steps:
      - uses: actions/checkout@v6
      - id: discover
        uses: ./.github/actions/discover-configs
        with:
          pattern: ".github/config/image/autogluon/1.6-sagemaker-*.yml"

  pipeline:
    needs: [check-changes, discover]
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJson(needs.discover.outputs.configs) }}
    uses: ./.github/workflows/autogluon.pipeline.yml
    with:
      config-file: ${{ matrix.config_file }}
      tag-suffix: pr-${{ github.event.pull_request.number }}
      build: ${{ needs.check-changes.outputs.build-change == 'true' }}
      run-sanity-test: ${{ needs.check-changes.outputs.build-change == 'true' || needs.check-changes.outputs.sanity-test-change == 'true' }}
      run-unit-test: ${{ (needs.check-changes.outputs.build-change == 'true' || needs.check-changes.outputs.unit-test-change == 'true') && endsWith(matrix.config_file, '-cpu.yml') }}
      run-security-test: ${{ needs.check-changes.outputs.build-change == 'true' }}
      run-telemetry-test: ${{ needs.check-changes.outputs.build-change == 'true' || needs.check-changes.outputs.telemetry-test-change == 'true' }}
      run-sagemaker-test: ${{ needs.check-changes.outputs.build-change == 'true' || needs.check-changes.outputs.sagemaker-test-change == 'true' }}
      release: false
    secrets: inherit
