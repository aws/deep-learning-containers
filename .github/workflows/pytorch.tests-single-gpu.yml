name: Reusable PyTorch Single-GPU Tests

on:
  workflow_call:
    inputs:
      config-file:
        description: "Path to image config YAML"
        required: true
        type: string
      image-uri:
        description: "Image URI to test. If empty, derives prod URI from config."
        required: false
        type: string
        default: ""

permissions:
  contents: read

jobs:
  preflight:
    runs-on:
      - codebuild-runner-${{ github.run_id }}-${{ github.run_attempt }}
        fleet:default-runner
        buildspec-override:true
    outputs:
      image-uri: ${{ steps.image.outputs.image-uri }}
      aws-account-id: ${{ steps.image.outputs.aws-account-id }}
      image-exists: ${{ steps.check.outputs.exists }}
    steps:
      - uses: actions/checkout@v6

      - name: Resolve image URI
        id: image
        uses: ./.github/actions/resolve-image-uri
        with:
          image-uri: ${{ inputs.image-uri }}
          ci-aws-account-id: ${{ vars.CI_AWS_ACCOUNT_ID }}
          prod-aws-account-id: ${{ vars.PROD_AWS_ACCOUNT_ID }}
          aws-region: ${{ vars.AWS_REGION }}
          config-file: ${{ inputs.config-file }}

      - name: Check image exists
        id: check
        uses: ./.github/actions/check-image-exists
        with:
          image-uri: ${{ steps.image.outputs.image-uri }}

  single-gpu-test:
    if: ${{ needs.preflight.outputs.image-exists == 'true' }}
    needs: [preflight]
    runs-on:
      - codebuild-runner-${{ github.run_id }}-${{ github.run_attempt }}
        fleet:x86-g6xl-runner
        buildspec-override:true
    steps:
      - uses: actions/checkout@v6

      - name: ECR login
        uses: ./.github/actions/ecr-authenticate
        with:
          aws-account-id: ${{ needs.preflight.outputs.aws-account-id }}
          aws-region: ${{ vars.AWS_REGION }}
          image-uri: ${{ needs.preflight.outputs.image-uri }}

      - name: Run single-GPU tests
        run: |
          # TODO: Remove NVIDIA_DISABLE_REQUIRE=true once the runner AMI ships
          # nvidia-container-toolkit >= 1.20. The current toolkit (1.19.1) has
          # a stale semver parser and compat table that reject the
          # `com.nvidia.cuda.requires=cuda>=13.3` label, even though the
          # runner's actual driver (580.159.04) exceeds CUDA 13.3's declared
          # minimum (580.65). Upstream fix landed in nvidia-container-toolkit
          # v1.20.0-rc.1 (2026-07-09). To remove: delete this comment + the
          # `-e NVIDIA_DISABLE_REQUIRE=true` flag, then verify CI passes on
          # the updated runner AMI.
          CONTAINER_ID=$(docker run -d --rm --gpus all -e NVIDIA_DISABLE_REQUIRE=true --shm-size=2g \
            --entrypoint /bin/bash \
            -v $(pwd):/workdir --workdir /workdir \
            ${{ needs.preflight.outputs.image-uri }} -c 'sleep infinity')
          echo "CONTAINER_ID=$CONTAINER_ID" >> $GITHUB_ENV
          docker exec ${CONTAINER_ID} pip install -r /workdir/test/requirements.txt -q
          docker exec -e PYTHONPATH=/workdir/test ${CONTAINER_ID} \
            pytest /workdir/test/pytorch/single_gpu/ -v
          docker kill ${CONTAINER_ID}
