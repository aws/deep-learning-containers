name: "Auto Release - SGLang SageMaker Ubuntu"

on:
  schedule:
    - cron: '00 18 * * 2,4'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  pipeline:
    uses: ./.github/workflows/sglang.pipeline.yml
    with:
      config-file: .github/config/image/sglang/sagemaker-ubuntu.yml
      tag-suffix: ${{ github.run_id }}
      release: true
    secrets: inherit
