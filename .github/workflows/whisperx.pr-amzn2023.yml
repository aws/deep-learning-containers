name: "PR - WhisperX AL2023"

on:
  workflow_dispatch:
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize]
    paths:
      - ".github/config/image/whisperx/*.yml"
      - ".github/workflows/whisperx.pipeline.yml"
      - ".github/workflows/whisperx.pr-amzn2023.yml"
      - ".github/workflows/whisperx.tests-ec2.yml"
      - ".github/workflows/whisperx.tests-sagemaker.yml"
      - "docker/whisperx/**"
      - "scripts/docker/whisperx/**"
      - "scripts/docker/common/**"
      - "scripts/docker/telemetry/**"
      - "scripts/ci/build/whisperx/**"
      - "test/sanity/**"
      - "test/telemetry/**"
      - "test/whisperx/**"
      - "test/security/data/ecr_scan_allowlist/whisperx/**"
      - "!docs/**"

permissions:
  contents: read
  pull-requests: read

jobs:
  gatekeeper:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          fetch-depth: 1
      - uses: ./.github/actions/pr-permission-gate

  check-changes:
    needs: [gatekeeper]
    runs-on: ubuntu-latest
    outputs:
      build-change: ${{ steps.changes.outputs.build-change }}
      sanity-test-change: ${{ steps.changes.outputs.sanity-test-change }}
      telemetry-test-change: ${{ steps.changes.outputs.telemetry-test-change }}
      ec2-test-change: ${{ steps.changes.outputs.ec2-test-change }}
      sagemaker-test-change: ${{ steps.changes.outputs.sagemaker-test-change }}
    steps:
      - uses: actions/checkout@v6
      - uses: dorny/paths-filter@v4
        id: changes
        with:
          filters: |
            build-change:
              - ".github/config/image/whisperx/*.yml"
              - "docker/whisperx/**"
              - "scripts/docker/whisperx/**"
              - "scripts/docker/common/**"
              - "scripts/docker/telemetry/**"
              - "scripts/ci/build/whisperx/**"
              - "test/security/data/ecr_scan_allowlist/whisperx/**"
            sanity-test-change:
              - "test/sanity/**"
            telemetry-test-change:
              - "test/telemetry/**"
            ec2-test-change:
              - "test/whisperx/ec2/**"
              - ".github/workflows/whisperx.tests-ec2.yml"
            sagemaker-test-change:
              - "test/whisperx/sagemaker/**"
              - ".github/workflows/whisperx.tests-sagemaker.yml"

  discover:
    needs: [gatekeeper]
    runs-on: ubuntu-latest
    outputs:
      configs: ${{ steps.discover.outputs.configs }}
    steps:
      - uses: actions/checkout@v6
      - id: discover
        uses: ./.github/actions/discover-configs
        with:
          pattern: ".github/config/image/whisperx/*.yml"

  pipeline:
    needs: [check-changes, discover]
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJson(needs.discover.outputs.configs) }}
    uses: ./.github/workflows/whisperx.pipeline.yml
    with:
      config-file: ${{ matrix.config_file }}
      tag-suffix: pr-${{ github.event.pull_request.number }}
      build: ${{ needs.check-changes.outputs.build-change == 'true' }}
      run-sanity-test: ${{ needs.check-changes.outputs.build-change == 'true' || needs.check-changes.outputs.sanity-test-change == 'true' }}
      run-security-test: ${{ needs.check-changes.outputs.build-change == 'true' }}
      run-telemetry-test: ${{ needs.check-changes.outputs.build-change == 'true' || needs.check-changes.outputs.telemetry-test-change == 'true' }}
      run-ec2-test: ${{ needs.check-changes.outputs.build-change == 'true' || needs.check-changes.outputs.ec2-test-change == 'true' }}
      run-sagemaker-test: ${{ needs.check-changes.outputs.build-change == 'true' || needs.check-changes.outputs.sagemaker-test-change == 'true' }}
      release: false
    secrets: inherit
