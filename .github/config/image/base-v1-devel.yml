# Base v1 devel (CUDA 12.9.1, amzn2023) Image Configuration
# This file contains all configuration for building, testing, and releasing the base-v1 devel image

# Image identification
image:
  name: "base-v1-devel"
  description: "Base v1 devel GPU image with CUDA 12.9.1, Python, and build tools on amzn2023"

# Build configuration
common:
  framework: "base_dlc"
  framework_version: "12.9.1"
  job_type: "general"
  python_version: "py313"
  cuda_version: "cu129"
  os_version: "amzn2023"
  customer_type: "ec2"
  arch_type: "x86"
  container_type: "devel-cuda"
  build_target: "devel"
  ci_tag: "cuda129-devel-amzn2023"
  device_type: "gpu"
  contributor: "None"

# Release configuration
release:
  release: true
  force_release: false
  public_registry: true
  private_registry: true
  enable_soci: true
  environment: gamma # gamma or production
