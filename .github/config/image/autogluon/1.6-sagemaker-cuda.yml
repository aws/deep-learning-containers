image:
  name: "autogluon-1.6-sagemaker-cuda"
  description: "AutoGluon 1.6 GPU training and inference for SageMaker"

metadata:
  framework: "autogluon"
  framework_version: "1.6.2"
  os_version: "amzn2023"
  customer_type: "sagemaker"
  platform: "sagemaker"
  arch_type: "x86"
  device_type: "gpu"
  job_type: "general"
  prod_image: "autogluon:1.6-cu133-amzn2023"

build:
  dockerfile: "docker/autogluon/Dockerfile.cuda"
  target: "sagemaker"
  base_image: "public.ecr.aws/deep-learning-containers/pytorch:2.13-cu133-amzn2023-sagemaker"
  python_version: "3.12"
  cuda_version: "13.3.0"
  dlc_major_version: "1"
  dlc_minor_version: "0"

release:
  # Gamma-testing the unified image before production.
  # Follow-up: publish the AutoGluon docs only after the production release succeeds.
  release: true
  force_release: false
  public_registry: false
  private_registry: true
  enable_soci: false
  environment: "gamma"
